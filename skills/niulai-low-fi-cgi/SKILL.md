---
name: niulai-low-fi-cgi
description: "生成原创的低成本早期 3D CGI、PS2 时代游戏过场、山寨儿童 VCD、Uncanny/Cursed CGI 与数字原始主义图像。用户提到 NiuLai、荒诞低精度 CGI、低多边形怪诞动画、廉价植绒角色、重复贴图森林或一本正经的诡异儿童叙事时使用。"
---

# NiuLai · 荒诞低精度 CGI

把角色与场景编译成原创的千禧年前后低预算 3D 动画截图：粗糙、天真、严肃，并因技术限制自然产生怪诞感。

## 工作流程

1. 完整读取 `references/style.json`。
2. 把 StyleSpec 视为可独立迁移的视觉合同；本 Skill 不依赖任何内置参考图。
3. 获取或合理推断 `environment_variables` 中的变量值。十个 Cookbook 基础变量与四个 NiuLai 扩展变量都必须被填入。
4. 用变量替换 `prompt_template` 中全部 `{{UPPER_SNAKE_CASE}}` 占位符；存在未替换项时停止生成并补全变量。
5. 应用全部 `style_fidelity_anchors`、构图、字幕、颜色、画面处理、虚拟摄影和 `rule_system` 约束。
6. 将完成后的正向提示词直接交给生图模型，不传入内置图片路径、图片 URL、base64 图片或 reference/init/control-image 字段。只有用户在当前请求中主动提供图片时，才把它作为本次内容或构图输入。
7. 生图模型支持 negative prompt 时，附加 `rule_system.avoid` 与 `rule_system.negative_prompt`。
8. 检查首轮结果是否仍像现代圆润黏土或毛毡手办；若人物轮廓过于丝滑、面数过高或贴图过细，必须先做一次纯文本定向降级：取消细分曲面，把肢体压到可见的 8–12 边圆柱与少量平面切面，并改用 32–128 px 的重复漫反射贴图、明显 UV 拉伸和有限纹理过滤，再交付结果。

## 迁移边界

- 迁移低精度建模、重复贴图、统一粗糙材质、简单灯光、朴素构图与 VCD 介质感。
- 新建主体、动作、道具、地点、对白和关系；不要复刻用户输入图或既有作品中的角色身份、人物面孔、原对白或镜头排列。
- 保持一本正经、天真且不自知的叙事语气。不要把画面做成主动堆梗、故意 glitch 或精致的互联网 meme。
- 不生成可识别的真实人物、公共人物、品牌、现有 IP、平台 UI、水印或来源角标。

## 输出检查

- 第一眼像低预算早期 3D 动画帧，而不是现代低多边形插画。
- 角色轮廓必须出现肉眼可见的直线段、平面切面与突兀接头；不能用高面数圆润模型加一层复古滤镜冒充低模。
- 关节僵硬、表情迟钝，材质像塑料、软陶或廉价植绒；粗糙感来自低清重复贴图、像素块、接缝和 UV 拉伸，不来自细腻均匀的现代程序噪点。
- 山川树木由少量基础模型重复搭建，草地与树冠能看出平铺贴图。
- 光线均匀、阴影简陋、景深缺失或略不自然；不要出现电影级 PBR、体积光与真实毛发。
- 如使用字幕，必须是原创文案，并呈现白字深色粗描边的低清 VCD 硬字幕效果。
